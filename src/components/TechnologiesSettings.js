import React from "react";

const TechnologiesSettings = ({isOpen}) => {
  return <div style={{ display: isOpen ? "block" : "none" }}>TechnologiesSettings</div>;
};

export default TechnologiesSettings;
